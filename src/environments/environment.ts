import { IEnvironment } from '../models/IEnvironment';

export const environment: IEnvironment = {
  envName: 'default',
  production: true,
  apiData: 'catch-prod',
  hmr: true,
  emailerEndpoint: '',
  recaptchaSiteKey: '',
};
