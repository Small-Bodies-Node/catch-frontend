import { IEnvironment } from '../app/models/IEnvironment';

export const environment: IEnvironment = {
  envName: 'default',
  production: false,
  apiData: 'mock',
  hmr: true,
  emailerEndpoint: '',
  recaptchaSiteKey: '',
};
