import { IEnvironment } from '../models/IEnvironment';

export const environment: IEnvironment = {
  //
  envName: 'default',
  production: false,
  apiData: 'mock',
  // apiData: 'catch-dev',
  hmr: true,
  emailerEndpoint: '',
  recaptchaSiteKey: '',
};
