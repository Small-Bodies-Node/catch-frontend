# Copy this file to ".env" and fill in values. Used by the dev API server (npm run dev:api).
# In production/SSR, provide these as real environment variables at runtime.

# === AWS Cognito (REQUIRED) ===
# AWS region where your Cognito User Pool resides, e.g. "us-east-1" or "eu-west-1"
COGNITO_REGION=us-east-1

# Cognito User Pool ID, e.g. "us-east-1_AbCdEfGHi"
COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX

# App client ID from your User Pool's App clients
COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx

# App client secret (ONLY if your app client is confidential / has a secret). If your client is public,
# leave this unset or empty. If set, our API will send SECRET_HASH for signup/login/confirm/refresh.
# NOTE for refresh: if a client secret is configured, the /api/auth/refresh endpoint expects { refreshToken, email }.
# The email is the same username used at signup/login and is required to compute SECRET_HASH.
#COGNITO_CLIENT_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# === Dev API server ===
# Port for the standalone dev API server (npm run dev:api). Angular dev server proxies /api -> this port (see proxy.conf.json)
PORT=3000

# === PM2 (optional, used by ./_pm2 wrapper) ===
# Name of the pm2 process (default used by _pm2: NG20)
# PM2_APP_NAME=NG20

# Number of days to retain logs via pm2-logrotate (default used by _pm2: 14)
# PM2_LOG_RETENTION_DAYS=14

# Optional absolute path to the node binary if not on PATH (e.g., /usr/bin/node)
# PM2_NODE_PATH=/usr/bin/node

# Entry file for production SSR server (default used by _pm2: dist/catch-frontend-ng20/server/server.mjs)
# PM2_START_FILE=dist/catch-frontend-ng20/server/server.mjs


AWS_ACCESS_KEY_ID=''
AWS_SECRET_ACCESS_KEY=''
AWS_REGION='us-east-1'
S3_BUCKET_NAME=''
S3_BUCKET_BASE_URL=''